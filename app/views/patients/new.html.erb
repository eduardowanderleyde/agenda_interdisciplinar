<main class="flex justify-center items-center min-h-[90vh]">
  <div class="bg-white rounded-xl shadow-xl p-10 max-w-xl w-full">

    <%= render "form", patient: @patient %>
    <div class="mt-8 text-center">
      <%= link_to "Voltar para pacientes", patients_path, class: "text-indigo-600 hover:underline" %>
    </div>
  </div>
</main>
