<nav class="bg-white border-b border-gray-200 px-4 py-2 flex items-center justify-between">
  <div class="flex items-center space-x-4">
    <%= link_to "Clínica Interdisciplinar", root_path, class: "text-xl font-bold text-indigo-700" %>
    <% if user_signed_in? %>
      <%= link_to "Pacientes", patients_path, class: "text-gray-700 hover:text-indigo-600 px-2" %>
      <%= link_to "Profissionais", professionals_path, class: "text-gray-700 hover:text-indigo-600 px-2" %>
      <%= link_to "Agendamentos", appointments_path, class: "text-gray-700 hover:text-indigo-600 px-2" %>
      <%= link_to "Sugestões de Horários", suggestions_path, class: "text-gray-700 hover:text-indigo-600 px-2", style: "font-weight:bold;color:#4f46e5;" %>
      <% if current_user.profissional? %>
        <%= link_to "Minhas Evoluções", evolutions_path, class: "text-gray-700 hover:text-indigo-600 px-2" %>
      <% end %>
    <% end %>
  </div>
  <div class="flex items-center space-x-2">
    <% if user_signed_in? %>
      <span class="text-gray-600 mr-2">Olá, <%= current_user.email %></span>
      <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "text-red-600 hover:underline bg-transparent border-none p-0 m-0", form: { style: "display:inline" } %>
    <% else %>
      <%= link_to "Entrar", new_user_session_path, class: "text-indigo-600 hover:underline" %>
    <% end %>
  </div>
</nav> 