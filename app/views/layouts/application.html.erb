<!DOCTYPE html>
<html>
  <head>
    <title>Agenda Interdisciplinar</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= javascript_pack_tag 'application', 'data-turbo-track': 'reload' %>

  </head>

  <body class="<%= devise_controller? ? '' : 'bg-gradient-to-br from-indigo-500 via-blue-600 to-purple-600 min-h-screen text-white' %>">
    <% unless devise_controller? %>
    <%= render NavigationComponent.new %>
      <main class="container mx-auto px-4 py-8">
    <% if notice %>
      <p class="text-green-700 bg-green-100 px-4 py-2 rounded mb-4 font-semibold shadow max-w-2xl mx-auto"> <%= sanitize(notice) %> </p>
    <% end %>
    <% if alert %>
        <div class="max-w-2xl mx-auto mb-6 p-4 bg-red-100 text-red-800 rounded-lg shadow font-semibold"> <%= alert %> </div>
    <% end %>

    <%= turbo_frame_tag "modal" %>
      <div class="max-w-4xl mx-auto bg-white/10 backdrop-blur-md rounded-3xl shadow-xl p-10 border border-white/20">
    <%= yield %>
      </div>
    </main>
      <% else %>
    <%= yield %>
  <% end %>

  </body>
</html>
