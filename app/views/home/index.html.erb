<div class="min-h-screen bg-gray-100">
  <div class="py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-8">
        <h1 class="text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl md:text-6xl">
          <span class="block">Sistema de Agendamento</span>
          <span class="block text-indigo-600">Clínica Interdisciplinar</span>
        </h1>
        <p class="mt-3 max-w-md mx-auto text-base text-gray-500 sm:text-lg md:mt-5 md:text-xl md:max-w-3xl">
          Gerencie consultas, acompanhe evoluções e organize sua agenda de forma eficiente.
        </p>
      </div>
      <% if user_signed_in? %>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">
          <div class="bg-white rounded shadow p-6">
            <div class="text-2xl font-bold text-indigo-700"><%= Patient.count %></div>
            <div class="text-gray-600">Pacientes</div>
          </div>
          <div class="bg-white rounded shadow p-6">
            <div class="text-2xl font-bold text-indigo-700"><%= Professional.count %></div>
            <div class="text-gray-600">Profissionais</div>
          </div>
          <div class="bg-white rounded shadow p-6">
            <div class="text-2xl font-bold text-indigo-700"><%= Appointment.where('start_time >= ?', Time.zone.now.beginning_of_day).count %></div>
            <div class="text-gray-600">Atendimentos Hoje</div>
          </div>
        </div>
        <div class="flex justify-center gap-4">
          <%= link_to "Ver Agenda", appointments_path, class: "rounded-md bg-indigo-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500" %>
          <%= link_to "Cadastrar Paciente", new_patient_path, class: "rounded-md bg-white px-4 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50" %>
        </div>
      <% else %>
        <div class="rounded-md shadow flex justify-center">
          <%= link_to "Entrar", new_user_session_path, class: "w-full flex items-center justify-center px-8 py-3 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 md:py-4 md:text-lg md:px-10" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
